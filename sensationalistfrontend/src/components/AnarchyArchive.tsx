import React from 'react';
import './AnarchyArchive.css'; // Import the CSS file for the Thrasher style
 // Import SVG as a React component

const AnimatedHeader: React.FC = () => {
  return (
    <div className="AnarchyArchive">
        <div className="AnarchyArchiveHeader">
            <h1>Anarchy Archive</h1>
            <p>Where the storys are spilt</p>
        </div>
    </div>
  );
};

export default AnimatedHeader;

